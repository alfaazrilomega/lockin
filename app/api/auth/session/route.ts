import { NextResponse } from 'next/server'

export async function GET(request: Request) {
  // Placeholder untuk mendapatkan session
  return NextResponse.json({ message: "Not implemented yet" }, { status: 501 })
}
