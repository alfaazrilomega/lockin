# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Agent Instructions

**Before acting in any conversation, agents must read `.claude/agents/lyra.md`** to understand specific behavior guidelines, preferences, and context for this project.

## Project Overview

**LockIn** is an all-in-one productivity and adaptive learning workspace built with Next.js 16 (App Router). It's a capstone project for Dicoding CC26-PS118 that solves context switching problems by integrating:
- Centralized calendar with project deadlines and meetings
- Smart notes with AI-powered transcription and summarization
- Project management with real-time progress tracking
- Interactive flashcards with spaced repetition (adaptive learning)

## Tech Stack

- **Frontend**: Next.js 16 (App Router), React 19, TypeScript 5, Tailwind CSS v4
- **UI Components**: Shadcn UI (Radix Vega style) with custom Lucide icons
- **Database**: Supabase (PostgreSQL) with Prisma ORM v7
- **Auth**: Supabase Auth (email/password, OAuth)
- **AI**: Groq (Whisper-large-v3) & OpenRouter (LLM)
- **Rich Text**: Slate.js for note editor
- **Calendar**: React Big Calendar

## Common Development Commands

```bash
# Development server (hot reload on http://localhost:3000)
npm run dev

# TypeScript type checking
npx tsc --noEmit

# Linting (ESLint with Next.js config)
npm run lint

# Build for production
npm run build

# Start production server
npm run start

# Prisma commands
npx prisma generate          # Generate Prisma client
npx prisma db push          # Sync schema to database (development)
npx prisma studio           # Open Prisma Studio (database GUI)

# Supabase local dev (if using Supabase CLI)
supabase start              # Start local Supabase instance
supabase stop               # Stop local Supabase instance
```

## Architecture & Key Patterns

### App Router Structure

```
app/
├── layout.tsx               # Root layout with ThemeProvider & Preloader
├── page.tsx                 # Landing page (server component)
├── globals.css              # Global styles + Tailwind
├── auth/                    # Authentication pages (sign-in, sign-up, etc.)
│   └── [route]/page.tsx
├── (dashboard)/             # Dashboard route group (authenticated)
│   ├── layout.tsx           # Dashboard layout with sidebar & top nav
│   ├── loading.tsx          # Loading state for dashboard
│   ├── dashboard/           # Main dashboard page
│   ├── overview/            # User overview page
│   ├── notes/               # Notes list & detail pages
│   └── projects/            # Projects list & detail pages
└── api/                     # API routes
    ├── ai/route.ts          # AI aggregation endpoint (meeting summary, flashcards)
    └── auth/                # Auth callbacks (Supabase)
```

### Database Layer

- **Prisma Client**: Located at `lib/prisma.ts` - singleton pattern with PostgreSQL adapter
- **Schema**: `prisma/schema.prisma` - defines User, Project, Task, Note, FlashcardDeck, Flashcard models
- **User sync**: Application users sync with Supabase Auth users via `id` (no UUID generation in Prisma)
- **Important**: User IDs come from Supabase Auth, not generated by Prisma

### Authentication

- **Supabase SSO**: Uses `@supabase/ssr` package for SSR support
- **Middleware**: `proxy.ts` handles route protection (redirects unauthenticated users to sign-in, authenticated users away from auth pages)
- **Client hook**: `hooks/use-auth.ts` provides auth state management for client components
- **Server utils**: `lib/supabase/server.ts` creates Supabase client for server components/actions
- **Browser utils**: `lib/supabase/client.ts` creates Supabase client for client components

### Server Actions

All database operations use **Server Actions** (`"use server"`) located in `lib/actions/`:
- `project.actions.ts` - Create project, get projects, manage members
- `task.actions.ts` - CRUD operations for tasks
- `note.actions.ts` - Note management with AI features
- `flashcard.actions.ts` - Flashcard deck & card operations
- `dashboard.actions.ts` - Dashboard statistics

Server actions automatically revalidate paths with `revalidatePath()` after mutations.

### AI Integration

- **OpenRouter client**: `lib/openrouter.ts` wraps OpenAI SDK with OpenRouter configuration
- **Features**:
  - Meeting transcription → summary
  - Text → flashcards (JSON schema enforced)
  - Audio transcription (Groq Whisper)
- **API route**: `app/api/ai/route.ts` provides unified endpoint for AI features
- **Environment**: Requires `OPENROUTER_API_KEY` and `NEXT_PUBLIC_APP_URL`

### Component Organization

```
components/
├── ui/                      # Shadcn UI components (pre-built)
│   ├── button.tsx
│   ├── card.tsx
│   ├── dialog.tsx
│   └── ...
├── layout/                  # Layout components
│   ├── sidebar.tsx         # Dashboard sidebar navigation
│   ├── top-nav.tsx         # Top navigation bar
│   ├── Preloader.tsx       # Loading overlay with theme detection
│   └── SmoothScrolling.tsx # Lenis smooth scroll wrapper (landing)
├── auth/                    # Authentication UI components
├── notes/                   # Note-specific components
├── shared/                  # Reusable composite components
│   ├── project-card.tsx
│   ├── note-card.tsx
│   ├── new-project-dialog.tsx
│   └── ...
└── theme-provider.tsx       # Next themes provider
```

### Styling & Theming

- **Tailwind CSS v4** with PostCSS
- **Shadcn UI** with Radix Vega style (dark mode ready)
- **CSS Variables**: Theme system uses CSS custom properties via `next-themes`
- **Fonts**: Inter (sans), Outfit (display), Playfair Display (serif) from Google Fonts
- **Global CSS**: `app/globals.css` contains base styles, Tailwind imports, and theme variables

### Performance Philosophy (Critical!)

From README - **Strict performance guidelines**:
- ❌ NO Framer Motion, WebGL, or 3D Canvas in Dashboard (only CSS transforms/opacity)
- ✅ Use View Transitions API and Speculation Rules for instant navigation
- ✅ Maintain INP < 200ms
- ✅ All animations must GPU-accelerated (via `tailwindcss-animate`)

Landing page (`app/page.tsx`) uses Lenis smooth scrolling and Motion (acceptable here).

## Testing Approach

No test framework is configured yet. If adding tests:
- Unit tests: Jest or Vitest with React Testing Library
- E2E tests: Playwright or Cypress
- Place test files alongside source: `component.test.tsx` or in `__tests__/` folders

## Environment Variables

Required in `.env` (see `.env.example` if exists):
- `DATABASE_URL` - PostgreSQL connection string (Supabase)
- `NEXT_PUBLIC_SUPABASE_URL` - Supabase project URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Supabase anon key
- `OPENROUTER_API_KEY` - OpenRouter API key
- `NEXT_PUBLIC_APP_URL` - Public app URL (for OpenRouter referer)

## Database Migrations

1. Update `prisma/schema.prisma`
2. Run: `npx prisma db push` (development) OR create migration: `npx prisma migrate dev --name description`
3. Generate client: `npx prisma generate`
4. Never commit `prisma/schema.prisma` changes without testing

## TypeScript Configuration

- **Path aliases**: `@/*` maps to root directory
- **Strict mode**: Enabled
- **JSX**: `react-jsx` (React 19)
- **Incremental build**: Enabled (`.tsbuildinfo`)

See `tsconfig.json` for full config.

## Important Files to Read

- `README.md` - Project overview and philosophy (must read)
- `components.json` - Shadcn UI config and aliases
- `prisma/schema.prisma` - Database schema
- `lib/types.ts` - Shared TypeScript types
- `proxy.ts` - Auth middleware (route protection logic)
- `lib/actions/*.ts` - Server action patterns

## Git Workflow

- Standard feature branch workflow
- Commit messages: Conventional Commits recommended (e.g., `feat:`, `fix:`, `docs:`)
- Do NOT commit `.env` or database secrets

## Deployment

- **Vercel**: Next.js 16 compatible (see Vercel docs)
- **Supabase**: Database and auth hosted on Supabase
- **Environment**: Set all env vars in deployment platform
- **Build command**: `npm run build`
- **Output directory**: `.next/`

## Known Pitfalls

1. **Prisma v7**: Uses adapter pattern - don't put `url` in schema. Connection string is in `lib/prisma.ts`
2. **Supabase SSR**: Must use `@supabase/ssr` helpers, not regular client in server components
3. **Middleware routing**: `proxy.ts` is the actual middleware (not `middleware.ts`) - configured in next.config.ts or via standalone
4. **AI token costs**: Using OpenRouter, monitor usage in dashboard
5. **Performance**: Dashboard pages focus on SSR and minimal JS; animations only in landing page

## Tools & Libraries

- **Icons**: Lucide React (`import { IconName } from 'lucide-react'`)
- **UI**: Shadcn UI components (`@/components/ui/*`)
- **Utilities**: `clsx` + `tailwind-merge` via `cn()` helper in `lib/utils.ts`
- **Date formatting**: `date-fns`
- **Animation**: `framer-motion` (landing only), `tailwindcss-animate` (UI)

## Code Style

- TypeScript strict mode
- Functional components with hooks
- Server Components by default, mark client components with `"use client"`
- Server Actions for all mutations (no direct API calls from client)
- Proper error handling with try/catch in server actions
- Use `revalidatePath()` after mutations to refresh cache

## Additional Resources

- Next.js 16 Docs: https://nextjs.org/docs
- Shadcn UI: https://ui.shadcn.com
- Prisma v7: https://prisma.io/docs
- Supabase Auth: https://supabase.com/docs/guides/auth

---

## AI AGENT OPERATING PROTOCOLS (PERMANENT & NON-NEGOTIABLE)

> These protocols were established after critical design and implementation failures.
> They MUST be followed without exception by every AI agent operating in this workspace.

### PROTOCOL 1: THE ABSOLUTE TRUTH OF THE USER
- The user's chronological descriptions, storyboards, voice transcripts, and logic are treated as **100% accurate ground truth**.
- You are **strictly prohibited** from overriding, second-guessing, or unilaterally "improving upon" the user's explicitly stated mechanics without first obtaining explicit written permission.
- If you disagree with an approach, you may **state your concern once**. If the user confirms their intent, you comply immediately.

### PROTOCOL 2: ZERO-SKIP VISION INGESTION (NO EXCEPTIONS)
**THE TRIGGER:** Any time images/screenshots are provided by the user, OR any time you autonomously capture DOM/browser screenshots for any UI, UX, frontend, or data task.

**THE MANDATE:** You are **FORBIDDEN** from relying solely on text descriptions when images are present. You **MUST** actively use your vision capabilities to analyze **EVERY SINGLE** provided image **before** writing any code or plan.

**EXTRACTION CHECKLIST** — before writing code, you must document from each image:
- [ ] Exact hex color codes for all significant UI colors (background, text, active/inactive states, all gradient stops)
- [ ] Typography scale (approximate font-size, weight, letter-spacing)
- [ ] Layout grid, spacing, and margin relationships
- [ ] Component state differences (visible vs. hidden, active vs. inactive)
- [ ] Gradient direction, hard/soft color stops, and any blending modes

Failure to perform this extraction **before** coding is a critical protocol violation.

### PROTOCOL 3: THE ANTI-HUBRIS MANDATE & DUAL-VERIFICATION
You are **strictly forbidden** from declaring you "completely understand" or are "100% certain" about any task. AI is inherently ambiguous and prone to errors. All assumptions must be explicitly surfaced and cross-examined.

**When operating in Plan Mode**, you MUST verify your assumptions TWICE before executing:

**Verification Layer 1 — Inside the Plan File:**
- List your comprehension of the task in explicit bullet points: `"- I understand that I will do X."`
- Conclude the section by explicitly asking: **"Are these points correct?"**

**Verification Layer 2 — In the Conversational Response:**
- Briefly reiterate your core understanding.
- Ask: **"Does this align perfectly with your intent?"**
- State: **"I am awaiting your verification before I execute, because my AI assessment may contain errors."**

**You may ONLY begin executing code after the user has provided explicit approval.**

### PROTOCOL 4: REAL ASSETS & AUTONOMOUS LOCALHOST VISUAL QA
**Established after repeated "blind coding" failures where lint passed but visuals were broken.**

1. **NO FAKE ASSETS**: Strictly forbidden from using placeholder URLs or assumed image paths. You MUST verify the actual filenames in `public/` or `src/assets/` using `list_dir` before writing any `src` attribute. If an image doesn't exist at that path, the layout fails visually even if lint passes.

2. **MANDATORY LOCALHOST VISUAL QA**: A passing linter does NOT mean GSAP physics work. You are forbidden from reporting completion of any UI/animation task based solely on `npm run lint`.

3. **THE QA WORKFLOW** (must be followed after every frontend/animation change):
   - Ensure the local dev server is running
   - Use autonomous browser/screenshot tools to visit `http://localhost:3000` (or the active port)
   - Visually inspect the rendered DOM — scroll to trigger GSAP animations
   - Verify with vision tools that images load and animations match the storyboard
   - Only AFTER visual confirmation passes may you run the linter and report back

4. **REPORTING**: You may NOT say "done" or "fixed" until you have attached a screenshot or visual proof that the specific mechanics are working on screen.
